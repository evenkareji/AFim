// const User = require('./user');
// const bcrypt = require('bcryptjs');
// const localStorategey = require('passport-local').Strategy;

// module.exports = function (passport) {
//   passport.use(
//     new localStorategey(async (username, password) => {
//       try {
//         const user = await findOne({ username: username });
//       } catch (err) {
//         console.log(err);
//       }
//     }),
//   );
// };
